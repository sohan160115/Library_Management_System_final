<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.library_management_system">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Faculty_List" />
        <activity android:name=".CSEBOOK" />
        <activity android:name=".CSE_BOOK" />
        <activity android:name=".PME_BOOK" />
        <activity android:name=".ChE_BOOK" />
        <activity android:name=".EEE_BOOK" />
        <activity android:name=".IPE_BOOK" />
        <activity android:name=".BME_BOOK" />
        <activity android:name=".TE_BOOK" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
    </application>

</manifest>